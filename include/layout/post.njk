---
layout: layout/article.njk
---

{%- css %}
    {% include "style/prism-theme.css" %}
    {% include "style/post.css" %}
{% endcss %}

{%- html "head" %}
    <meta name="og:type" content="article" />
    <meta name="article:published_time" content="{{ date | readableDate }}" />
    <meta name="article:author" content="https://{{ config.hostname }}/about" />
    {% for tag in tags | filterTags %}
        <meta name="article:tag" content="{{ tag }}" />
    {% endfor %}
{% endhtml %}

<dl class="table">
    <dt>Published</dt>
    <dd>
        <time class="dt-published" datetime="{{ date }}">{{ date | readableDate }}</time>
    </dd>
    {% if updated %}
        <dt>Updated</dt>
        <time class="dt-updated" datetime="{{ updated }}">{{ updated | readableDate }}</time>
    {% endif %}
    <dt>Tags</dt>
    <dd>
        <ul class="p-category">
            {% for tag in tags | filterTags %}
                <li>
                    <a href="/tags/{{ tag }}">{{ tag }}</a>
                </li>
            {% endfor %}
        </ul>
    </dd>
    <dt>License</dt>
    <dd>{{ license }}</dd>
</dl>

<h1 class="p-name title">{{ title }}</h1>

<section class="e-content">
    {{ content | safe }}
</section>
